Drop a narrator reference clip here as `narrator.wav` (10s+, clean, single speaker,
>=24 kHz), matching `voice: narrator` in series.yaml. Use `prosodia voice-prep` to cut
one from a longer recording. The renderer clones it zero-shot and reuses the same clip
across every episode for a consistent voice.
